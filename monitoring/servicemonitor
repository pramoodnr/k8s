apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: portfolio-api
  namespace: devops-portfolio
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: portfolio-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
